/// The standard_library module, which provides a wrapper over a HashMap to define all of the functions in the standard library.
pub mod standard_library;

/// The function module, which contains the definition of functions.
mod function;

/// The io module, which contains standard library functions related to IO.
mod io;